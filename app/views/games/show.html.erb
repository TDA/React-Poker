<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @game.title %>
</p>

<p>
  <strong>Date:</strong>
  <%= @game.date %>
</p>

<p>
  <strong>Totalplayers:</strong>
  <%= @users.count %>
</p>

<%= react_component 'Game', { game: @game, players: @users } %>

<hr>
<%= link_to 'Edit', edit_game_path(@game), class: 'btn btn-link' %> |
<%= link_to 'Back', games_path, class: 'btn btn-link' %>
